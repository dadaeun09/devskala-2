apiVersion: tekton.dev/v1
kind: Task
metadata:
    name: hello-task
    namespace: tekton-demo
spec:
    steps:
        - name: print-message
          image: alpine:3.20
          script: |
              #!/bin/sh
              echo "Hello from Tekton!"
